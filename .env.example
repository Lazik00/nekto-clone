# Environment Configuration
ENVIRONMENT=development
DEBUG=True

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=nekto
POSTGRES_PASSWORD=nekto_secure_password_123
POSTGRES_DB=nekto

# SQLite (for local development)
USE_SQLITE=True
SQLITE_DB_PATH=nekto.db

# Redis Configuration (Optional - falls back to in-memory cache)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Security & JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars-abcdefghijklmnop
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080
JWT_REFRESH_EXPIRE_DAYS=30

# TURN/STUN Servers
TURN_SERVER=turn:turn:3478
TURN_USERNAME=nekto
TURN_PASSWORD=super_secret_password_123
STUN_SERVER=stun:stun.l.google.com:19302

# API Settings
CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173", "http://localhost:8000", "http://127.0.0.1:8000", "http://localhost:3000"]
ALLOWED_HOSTS=["localhost", "127.0.0.1"]
WORKERS=4

# Matchmaking
MATCH_TIMEOUT_SECONDS=120
MAX_MATCHES_PER_HOUR=10
MESSAGE_RETENTION_DAYS=30

# Project Settings
PROJECT_NAME=Nekto Clone
API_V1_PREFIX=/api/v1

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Email (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
ADMIN_EMAIL=admin@nekto.local

